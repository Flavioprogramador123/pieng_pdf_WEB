# Pasta obsoleto

Configurações antigas **não usadas** no deploy atual.

**Deploy ativo:** raiz do projeto → `vercel.json` + `api/index.py` (Vercel).

| Arquivo | Era usado para |
|---------|----------------|
| `netlify.toml` | Netlify + proxy Railway |
| `railway.toml` | API no Railway |

Ver **[VERCEL.md](../VERCEL.md)**.

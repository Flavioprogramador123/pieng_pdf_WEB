/**
 * Desligue aqui para reverter a evolução do leitor sem apagar código.
 * Rollback rápido: tudo false + deploy (volta ao comportamento v27).
 */
export const FEATURES = {
  readingToolbar: true,
  officeReader: true,
  defaultAppPrompt: true,
};

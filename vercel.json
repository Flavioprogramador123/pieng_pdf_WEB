{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "src/static",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://SUA-API.up.railway.app/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
